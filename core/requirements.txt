# Runtime deps for deployment (mirrors pyproject.toml [project].dependencies).
# Railpack/Railway installs these; local dev can still use `pip install -e '.[dev]'`.
fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
pydantic-settings>=2.5
anthropic>=0.40
langgraph>=0.2.50
langsmith>=0.1.140
clerk-backend-api>=6.0
sqlalchemy[asyncio]>=2.0
asyncpg>=0.30
alembic>=1.13
